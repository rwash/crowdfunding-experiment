<%= form_for(@experiment) do |f| %>
  <% if @experiment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@experiment.errors.count, "error") %> prohibited this session from being saved:</h2>

      <ul>
      <% @experiment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :return_credits %>
    <%= f.radio_button :return_credits, true %> Credits are returned to users if proejct is not funded.<br>
    <%= f.radio_button :return_credits, false %> Credits ARE NOT returned to users if project is funded.
  </div>
  <br>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
