<h1>Instructions</h1>
<%= render "shared/instructions" %>
<strong>Times viewed: <%= current_user.times_viewed_instructions %></strong>
<br>
<br>
<%= link_to "Go Back", :back, :class => 'btn-success btn-large'  if current_experiment.started? %>
<%= link_to "Start the First Round", round_waiting_path(@first_round), :class => 'btn-success btn-large' unless current_experiment.started?%>
